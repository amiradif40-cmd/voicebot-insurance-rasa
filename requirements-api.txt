# Dépendances de l'ORCHESTRATEUR VOCAL (main.py)
# -> à installer dans un venv SÉPARÉ de celui de Rasa, pour éviter les
#    conflits (Rasa verrouille numpy/tensorflow/pydantic<2, alors que
#    Whisper embarque torch).
#
#    python3.10 -m venv venv-api
#    source venv-api/bin/activate
#    pip install -r requirements-api.txt
#
#    Whisper a aussi besoin du binaire ffmpeg installé au niveau du SYSTÈME
#    (pas via pip) :
#      Ubuntu/Debian : sudo apt install ffmpeg
#      macOS (brew)  : brew install ffmpeg
#      Windows       : choco install ffmpeg

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9
requests>=2.31.0
gTTS>=2.5.0
openai-whisper
pydantic>=2.0
